========================================================================
  SAMS - Seeded Multi-Tenant Demo Data
  Generated by: back/seed_data.py
  All Passwords : 123321  (including every super_admin)
========================================================================

Email domains use the tenant_id with underscores stripped (Keycloak's
realm rejects an underscore in the email domain) -- tenant_a -> tenanta.com,
tenant_b -> tenantb.com, tenant_c -> tenantc.com.

------------------------------------------------------------------------
GLOBAL CONTROL PLANE (SUPER ADMIN)
------------------------------------------------------------------------
  Every row in public.super_admins gets a real, visible account in every
  tenant below (role=super_admin), auto-provisioned by
  app/core/database.py::_initialize_tenant_tables(). As of this writing
  that includes at least:
    sa@desk.com

------------------------------------------------------------------------
PER TENANT (identical shape in tenant_a, tenant_b, tenant_c)
------------------------------------------------------------------------
Replace {d} below with the tenant's email domain (tenanta.com / tenantb.com
/ tenantc.com).

== ADMIN & MANAGEMENT ==
  School Admin : admin.a@{d}      (e.g. admin.a@tenanta.com)
  Manager      : manager.a@{d}

== LEVELS (4) ==
  Grade 1, Grade 2, Grade 3, Grade 4

== CLASSES (12 -- 3 sections per grade) ==
  "Grade N Section A/B/C" for N in 1..4

== TEACHERS (15) ==
  teacher.1@{d} ... teacher.15@{d}
  (assigned round-robin as head teacher across the 12 classes)

== STUDENTS (60 -- 5 per class) ==
  student.1@{d} ... student.60@{d}
  Each is linked to up to 2 of the 8 parents below.

== PARENTS (8) ==
  parent.1@{d} ... parent.8@{d}

== EVENTS (8 -- cycling through every lifecycle state) ==
  "Trip 1 (draft)"     status: DRAFT
  "Trip 2 (proposed)"  status: PROPOSED
  "Trip 3 (approved)"  status: APPROVED
  "Trip 4 (published)" status: PUBLISHED
  "Trip 5 (draft)"     status: DRAFT
  "Trip 6 (proposed)"  status: PROPOSED
  "Trip 7 (approved)"  status: APPROVED
  "Trip 8 (published)" status: PUBLISHED
  Each is mapped to one class, with 2 sample resource lines (with costs)
  and 3 sample enrollments (each with a paid payment).

== SCHOOL PROFILE / ONBOARDING ==
  Legal name "School {tenant_id}", already activated (profile committed,
  structure committed, curriculum locked). One real admin contact on file
  (role_title "School Administrator", email admin.a@{d}) so the app's
  "Contact Administrator" pending-role screen shows a working address
  instead of a placeholder.

------------------------------------------------------------------------
Re-running `python back/seed_data.py` truncates and reseeds every tenant
listed above from scratch -- do not run it against data you want to keep.
========================================================================
