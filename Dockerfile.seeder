FROM python:3.12-slim

WORKDIR /workspace

# Install dependencies (layer caching optimization)
RUN pip install --no-cache-dir asyncpg httpx

# Copy the seed data script
COPY seed_data.py .

CMD ["python", "seed_data.py"]
